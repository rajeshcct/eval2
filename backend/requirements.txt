# All backend dependencies (fastapi, uvicorn, httpx) are now in the
# project root requirements.txt — install from there:
#
#   pip install -r requirements.txt   (run from the project root)
#
# This file is kept as a placeholder only.
